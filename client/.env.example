# URL of your deployed API server (Render), including /api
# For local development, the default in src/lib/api.js already points to
# http://localhost:8080/api, so this file only matters for production builds.
VITE_API_URL=https://your-render-service.onrender.com/api
